taking_it_all_perk = {
	lifestyle = martial_lifestyle
	tree = plunderer
	position = { 1 0 }
	icon = node_martial
	
	auto_selection_weight = {
		value = 11
		if = {
			limit = {
				has_education_martial_trigger = yes
			}
			add = 1989
		}
		if = {
			limit = {
				has_focus = martial_chivalry_focus
			}
			multiply = 5
		}
		if = {
			limit = {
				can_start_new_lifestyle_tree_trigger = no
			}
			multiply = 0
		}
	}

	name = {
		first_valid = {
			triggered_desc = {
				trigger = { is_landless_adventurer = yes }
				desc = taking_it_all_perk_adventurer_name
			}
			desc = taking_it_all_perk_name
		}
	}

	government_character_modifier = {
		flag = government_is_landless_adventurer
		invert_check = yes
		raid_speed = 0.25
	}
	
	effect = { 
		if = { # Allows you to raid if you can't
			limit = {
				NOT = { government_has_flag = government_is_landless_adventurer }
			}
			custom_description_no_bullet = {
				text = taking_it_all_perk_effect
			}
		}
		if = { # Access to the plunder travel option
			limit = {
				government_has_flag = government_is_landless_adventurer
			}	
			custom_description_no_bullet = {
				text = adventurer_taking_it_all_perk_effect
			}
		}
		if = {
			limit = { culture = { has_cultural_parameter = plunderer_xp_rebate } } ###EK EDIT: new parameter
			add_martial_lifestyle_xp = 330
		}
	}
}

open_seas_perk = {
	lifestyle = martial_lifestyle
	tree = plunderer
	position = { 0 1 }
	icon = node_martial
	
	parent = taking_it_all_perk

	character_modifier = {
		embarkation_cost_mult = -0.25
	}
	
	government_character_modifier = {
		flag = government_is_landless_adventurer
		no_disembark_penalty = yes
		sea_travel_danger = -10
		coastal_sea_travel_danger = -10
	}
	
	effect = { # Allows you to raid oversea if you can't
		if = {
			limit = {
				NOT = { government_has_flag = government_is_landless_adventurer }
			}
			custom_description_no_bullet = { # Allows to raid oversea
				text = open_seas_perk_effect
			}
		}
		if = {
			limit = { culture = { has_cultural_parameter = plunderer_xp_rebate } } ###EK EDIT: new parameter
			add_martial_lifestyle_xp = 330
		}
	}
}

bountiful_offerings_perk = {
	lifestyle = martial_lifestyle
	tree = plunderer
	position = { 2 1 }
	icon = node_martial
	
	parent = taking_it_all_perk

	name = {
		first_valid = {
			triggered_desc = {
				trigger = { is_landless_adventurer = yes }
				desc = bountiful_offerings_perk_adventurer_name
			}
			desc = bountiful_offerings_perk_name
		}
	}

	government_character_modifier = {
		flag = government_is_landless_adventurer
		martial_scheme_phase_duration = -10
	}	
	
	effect = { 
		if = { # You get piety from raids
			limit = {
				NOT = { government_has_flag = government_is_landless_adventurer }
			}
			custom_description_no_bullet = { # Granting pardon to a criminal gives a hook
				text = bountiful_offerings_perk_effect
			}
		}
		if = { # More Raiding Contracts
			limit = {
				government_has_flag = government_is_landless_adventurer
			}	
			custom_description_no_bullet = {
				text = adventurer_bountiful_offerings_perk_effect
			}
		}
		if = {
			limit = { culture = { has_cultural_parameter = plunderer_xp_rebate } } ###EK EDIT: new parameter
			add_martial_lifestyle_xp = 330
		}
	}
}

hard_bitten_perk = {
	lifestyle = martial_lifestyle
	tree = plunderer
	position = { 1 2 }
	icon = node_martial
	
	parent = open_seas_perk
	parent = bountiful_offerings_perk

	character_modifier = {
		hostile_county_attrition = -0.2
		supply_duration = 0.2		
	}

	government_character_modifier = {
		flag = government_is_landless_adventurer
		invert_check = yes
		hostile_county_attrition_raiding = -0.2
	}
	
	government_character_modifier = {
		flag = government_is_landless_adventurer
		negate_health_penalty_add = 0.2
		negate_prowess_penalty_add = 2
	}
	
	effect = {
		if = {
			limit = { culture = { has_cultural_parameter = plunderer_xp_rebate } } ###EK EDIT: new parameter
			add_martial_lifestyle_xp = 330
		}
	}
}

prisoners_and_loot_perk = {
	lifestyle = martial_lifestyle
	tree = plunderer
	position = { 0 3 }
	icon = node_martial
	
	parent = hard_bitten_perk
	
	effect = { # Higher chance of getting prisoners from raids and sieges
		if = {
			limit = {
				NOT = { government_has_flag = government_is_landless_adventurer }
			}
			custom_description_no_bullet = { # Granting pardon to a criminal gives a hook
				text = prisoners_and_loot_perk_effect
			}
		}
		if = {
			limit = {
				government_has_flag = government_is_landless_adventurer
			}	
			custom_description_no_bullet = { # Increased Request chance based on dread
				text = adventurer_prisoners_and_loot_perk_effect
			}
		}
		if = {
			limit = { culture = { has_cultural_parameter = plunderer_xp_rebate } } ###EK EDIT: new parameter
			add_martial_lifestyle_xp = 330
		}
	}
}

loot_and_prisoners_perk = {
	lifestyle = martial_lifestyle
	tree = plunderer
	position = { 2 3 }
	icon = node_martial
	
	parent = hard_bitten_perk
	
	effect = { 
		if = { # Raids and Sieges give more loot
			limit = {
				NOT = { government_has_flag = government_is_landless_adventurer }
			}
			custom_description_no_bullet = { # Granting pardon to a criminal gives a hook
				text = loot_and_prisoners_perk_effect
			}
		}
		if = { # Sieges give more loot
			limit = {
				government_has_flag = government_is_landless_adventurer
			}	
			custom_description_no_bullet = { # Increased Request chance based on dread
				text = adventurer_loot_and_prisoners_perk_effect
			}
		}
		if = {
			limit = { culture = { has_cultural_parameter = plunderer_xp_rebate } } ###EK EDIT: new parameter
			add_martial_lifestyle_xp = 330
		}
	}
}

tales_of_glory_perk = {
	lifestyle = martial_lifestyle
	tree = plunderer
	position = { 1 4 }
	icon = node_martial
	
	parent = prisoners_and_loot_perk
	parent = loot_and_prisoners_perk
	
	effect = { # Gain renown after every battle
		custom_description_no_bullet = {
			text = tales_of_glory_perk_effect
		}
		if = {
			limit = { culture = { has_cultural_parameter = plunderer_xp_rebate } } ###EK EDIT: new parameter
			add_martial_lifestyle_xp = 330
		}
	}
}

plunderer_perk = {
	lifestyle = martial_lifestyle
	tree = plunderer
	position = { 1 5 }
	icon = trait_plunderer
	
	parent = tales_of_glory_perk
	
	trait = plunderer
	effect = {
		add_trait_force_tooltip = plunderer
		if = {
			limit = { culture = { has_cultural_parameter = plunderer_xp_rebate } } ###EK EDIT: new parameter
			add_martial_lifestyle_xp = 500
		}
	}
}
